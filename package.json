{
  "name": "true-app",
  "private": true,
  "scripts": {
    "build:app": "node scripts/build-www.mjs",
    "sync": "npm run build:app && npx cap sync",
    "android": "npm run sync && npx cap open android",
    "assets": "node scripts/make-app-assets.mjs",
    "release:android": "node scripts/release-android.mjs",
    "store": "node scripts/make-store-assets.mjs",
    "test": "playwright test"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/filesystem": "^8.1.3",
    "@capacitor/ios": "^8.5.2",
    "@capacitor/local-notifications": "^8.3.1",
    "@capgo/capacitor-native-biometric": "^8.6.11",
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.5.2",
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.28.2"
  }
}
